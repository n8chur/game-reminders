<Window x:Class="GameReminders.App.NewReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New reminder" Height="330" Width="520" MinHeight="300" MinWidth="440"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="New reminder" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,18" />
        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="Game" FontWeight="SemiBold" Margin="0,0,0,5" />
            <ComboBox x:Name="GamePicker" Height="36" Padding="8,5"
                      SelectionChanged="Input_Changed">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <TextBlock Grid.Row="2" Text="Reminder" FontWeight="SemiBold" Margin="0,0,0,5" />
        <TextBox x:Name="MessageText" Grid.Row="3" MinHeight="82" Padding="9,7"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 TextChanged="Input_Changed" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" IsCancel="True" />
            <Button x:Name="CreateButton" Content="Create reminder" IsDefault="True"
                    Click="Create_Click" IsEnabled="False" MinWidth="128" />
        </StackPanel>
    </Grid>
</Window>
