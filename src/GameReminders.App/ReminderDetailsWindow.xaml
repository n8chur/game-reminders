<Window x:Class="GameReminders.App.ReminderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reminder" Height="430" Width="560" MinHeight="400" MinWidth="460"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText" Text="Reminder" FontSize="24" FontWeight="SemiBold"
                   Margin="0,0,0,18" />

        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="Game" FontWeight="SemiBold" Margin="0,0,0,5" />
            <ComboBox x:Name="GamePicker" Height="36" Padding="8,5"
                      SelectionChanged="Input_Changed">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayName}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Border x:Name="GameDisplaySurface" MinHeight="36" Padding="9,5"
                    Visibility="Collapsed" CornerRadius="4"
                    Background="{DynamicResource SurfaceMutedBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <TextBlock x:Name="GameDisplayText" VerticalAlignment="Center" TextWrapping="Wrap" />
            </Border>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Reminder" FontWeight="SemiBold" Margin="0,0,0,5" />
        <TextBox x:Name="MessageText" Grid.Row="3" MinHeight="120" Padding="9,7"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 TextChanged="Input_Changed" />

        <TextBlock x:Name="CreatedText" Grid.Row="4" Margin="0,12,0,0"
                   Foreground="{DynamicResource SecondaryTextBrush}" />

        <StackPanel x:Name="EditButtons" Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" IsCancel="True" />
            <Button x:Name="SaveButton" Content="Save" IsDefault="True"
                    Style="{StaticResource PrimaryActionButtonStyle}"
                    Click="Save_Click" MinWidth="96" />
        </StackPanel>

        <StackPanel x:Name="ViewButtons" Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,18,0,0" Visibility="Collapsed">
            <Button Content="Close" IsCancel="True" MinWidth="96" />
        </StackPanel>
    </Grid>
</Window>
